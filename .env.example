# =============================================================================
# Multi-Formula Stock Screening Bot - Environment Configuration
# =============================================================================
# Copy this file to .env and fill in your actual values.
# NEVER commit .env to version control!
# =============================================================================

# -----------------------------------------------------------------------------
# Discord Webhook URL
# -----------------------------------------------------------------------------
# Required for sending stock pick notifications to your Discord channel.
#
# How to obtain:
# 1. Open Discord and go to your server
# 2. Click the gear icon next to the channel where you want notifications
# 3. Go to "Integrations" > "Webhooks"
# 4. Click "New Webhook" or "Create Webhook"
# 5. Give it a name (e.g., "Stock Screener Bot")
# 6. Click "Copy Webhook URL"
#
# The URL format is:
# https://discord.com/api/webhooks/{webhook_id}/{webhook_token}
# -----------------------------------------------------------------------------
DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/your_webhook_id/your_webhook_token

# -----------------------------------------------------------------------------
# Formula Enable/Disable Toggles
# -----------------------------------------------------------------------------
# Set to "true" to enable a formula, "false" to disable.
# At least one formula must be enabled for the bot to run.
#
# Available formulas:
#   - Magic Formula: Combines Earnings Yield + Return on Capital (Greenblatt)
#   - Piotroski F-Score: 9-point fundamental strength scoring
#   - Graham Number: Intrinsic value with margin of safety
#   - Acquirer's Multiple: EV/EBIT deep value metric
#   - Altman Z-Score: Bankruptcy risk assessment (Safe Zone only)
#   - Reddit Momentum: Social sentiment from r/Wallstreetbets (Tradestie API)
#
# Example: Enable only Magic Formula and Piotroski F-Score
# ENABLE_MAGIC_FORMULA=true
# ENABLE_PIOTROSKI=true
# ENABLE_GRAHAM=false
# ENABLE_ACQUIRER=false
# ENABLE_ALTMAN=false
# ENABLE_REDDIT_MOMENTUM=false
# -----------------------------------------------------------------------------
ENABLE_MAGIC_FORMULA=true
ENABLE_PIOTROSKI=true
ENABLE_GRAHAM=true
ENABLE_ACQUIRER=true
ENABLE_ALTMAN=true
ENABLE_REDDIT_MOMENTUM=true

# =============================================================================
# GitHub Actions Secrets Setup
# =============================================================================
# When deploying via GitHub Actions, add these as repository secrets:
#
# 1. Go to your GitHub repository
# 2. Click "Settings" > "Secrets and variables" > "Actions"
# 3. Click "New repository secret"
# 4. Add the secrets:
#    - Name: DISCORD_WEBHOOK_URL  Value: (your Discord webhook URL)
#    - Name: ENABLE_MAGIC_FORMULA  Value: true
#    - Name: ENABLE_PIOTROSKI  Value: true
#    - Name: ENABLE_GRAHAM  Value: true
#    - Name: ENABLE_ACQUIRER  Value: true
#    - Name: ENABLE_ALTMAN  Value: true
#    - Name: ENABLE_REDDIT_MOMENTUM  Value: true
#
# The GitHub Actions workflow will automatically use these secrets.
# =============================================================================
