# =============================================================================
# Magic Formula DCA Bot - Environment Configuration
# =============================================================================
# Copy this file to .env and fill in your actual values.
# NEVER commit .env to version control!
# =============================================================================

# -----------------------------------------------------------------------------
# Discord Webhook URL
# -----------------------------------------------------------------------------
# Required for sending stock pick notifications to your Discord channel.
#
# How to obtain:
# 1. Open Discord and go to your server
# 2. Click the gear icon next to the channel where you want notifications
# 3. Go to "Integrations" > "Webhooks"
# 4. Click "New Webhook" or "Create Webhook"
# 5. Give it a name (e.g., "Magic Formula Bot")
# 6. Click "Copy Webhook URL"
#
# The URL format is:
# https://discord.com/api/webhooks/{webhook_id}/{webhook_token}
# -----------------------------------------------------------------------------
DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/your_webhook_id/your_webhook_token

# =============================================================================
# GitHub Actions Secrets Setup
# =============================================================================
# When deploying via GitHub Actions, add this as a repository secret:
#
# 1. Go to your GitHub repository
# 2. Click "Settings" > "Secrets and variables" > "Actions"
# 3. Click "New repository secret"
# 4. Add the secret:
#    - Name: DISCORD_WEBHOOK_URL  Value: (your Discord webhook URL)
#
# The GitHub Actions workflow will automatically use this secret.
# =============================================================================
